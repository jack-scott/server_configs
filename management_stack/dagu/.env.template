# Dagu Configuration
# Path where Dagu configuration will be stored
DAGU_CONFIG_PATH=./config

# Path where DAGs (workflow definitions) will be stored
DAGU_DAGS_PATH=./dags

# Network Configuration
# Port to expose Dagu UI on the host
DAGU_PORT=8080

# Host binding (0.0.0.0 for all interfaces)
DAGU_HOST=0.0.0.0

# Timezone
TZ=UTC
